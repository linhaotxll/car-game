precision mediump float;

// varying vec2 v_uv;
// uniform sampler2D u_texture;
// uniform vec3 u_color;

void main(){
  // vec2 vUv = v_uv;
  // vUv.y = 1.0 - vUv.y;

  // vec4 textureColor = texture2D(u_texture, vUv);
  // float alpha = 1.0 - textureColor.r;
  // alpha = smoothstep(0.16, 0.8, alpha);
  // alpha = alpha * .8;


  gl_FragColor = vec4(.0, .0, .0, .0);
}
